language: php

php:
  - 5.6
  - 5.5
  - 5.4
  - hhvm

before_script:
  - composer self-update
  - composer install -n --prefer-dist
  - ./vendor/bin/codecept build

script:
  - ./vendor/bin/codecept run